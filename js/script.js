// js/script.js (Utilitaires généraux)

/**
 * Fonction pour le mode debug, si nécessaire
 */
function logToConsole(message) {
    console.log(`[SITE GLOBAL] ${message}`);
}

// Exemple : Détection de la résolution d'écran (si jamais besoin)
// document.addEventListener('DOMContentLoaded', () => {
//     logToConsole(`Largeur d'écran: ${window.innerWidth}px`);
// });
